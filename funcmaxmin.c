#include <stdio.h>
int maxmin(int array[])
{
    
}
int main()
{
    int n;
    printf("enter the number of array:\n");
    scanf("%d",&n);
    int array[n];
    for(int i=0;i<n;i++)
    {
        scanf("%d",array[i]);
    }
    return 0;
}